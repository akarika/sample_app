 <%= form_for current_user.relationships.build(followed_id: @user.id), remote: true do |f| %>
<div><%= hidden_field_tag :followed_id %></div>
  <%= f.submit "Suivre", class: "btn btn-primary"%>
<% end %>

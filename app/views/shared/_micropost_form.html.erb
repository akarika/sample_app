<%= form_for @micropost do |f| %>
  <%= render "shared/error_messages", object: f.object  %>
  <div class="field">
    <%= f.text_area :content, placeholder:"Nouveau micropost..." %>
  </div>
  <%= f.submit"Soumettre", class:"btn btn-primary"%>

<% end %>
